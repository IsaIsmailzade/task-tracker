spring:
  application:
    name: task-tracker-backend

  datasource:
    url: jdbc:postgresql://db:5432/task_tracker
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false

  kafka:
    bootstrap-servers: kafka:29092
    topic: EMAIL_SENDING_TASKS
    template:
      default-topic: EMAIL_SENDING_TASKS

task-tracker-backend:
  jwt:
    secret_key: 31d16798f9eb4e38b82687846e5f7d67a12a5e016244a730aeff5a2a26db0da1
    access_token_expiration: 1d

logging:
  level:
    root: info
